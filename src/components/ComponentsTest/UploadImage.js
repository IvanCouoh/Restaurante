import React from 'react';
import Layout from '../../components/Layout/Index';

const UploadImage = () => {
    return (
        <Layout>
            
        </Layout>
    );
};

export default UploadImage;